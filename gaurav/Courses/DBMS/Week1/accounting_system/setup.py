from setuptools import setup, find_packages

setup(
    name='accounting_system',
    version='1.0.0',
    description='A simple accounting system for handling accounts and transactions',
    author='Gaurav Rana',
    packages=find_packages(),  # Automatically find the package directory
    install_requires=[],  # Add any external packages your system depends on
)